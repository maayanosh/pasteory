name: CI

on: push

jobs:
  test:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Build app
        run: make build/PasteClone
      - name: Run tests
        run: make test
